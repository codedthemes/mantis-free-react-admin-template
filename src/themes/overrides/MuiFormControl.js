// ==============================|| OVERRIDES - INPUT LABEL ||============================== //

export default function MuiFormControl() {
  return {
    MuiFormControl: {
      styleOverrides: {
        root: {
          // marginBottom: theme.spacing(2)
        }
      }
    }
  };
}
